[project]
name = "sdlc-agent-team"
version = "0.1.0"
description = "Collaborative Software Engineering Group"
requires-python = ">=3.10, <3.15"
dependencies = [
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.buil_meta"

[tools.setuptools.packages.find]
where = ["src"]